# 生产环境配置覆盖

spring:
  datasource:
    hikari:
      maximum-pool-size: 10
      minimum-idle: 3
      connection-timeout: 5000
      max-lifetime: 600000
      idle-timeout: 300000

springdoc:
  api-docs:
    enabled: false
